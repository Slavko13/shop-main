extends ../layout/main

block variables
    - var title = 'Webpack + Pug';
    - var description = "Description for Pug";
    - var pageClass = 'index-page';

block content
    section
        .container
            h1 GG HLF
            p Hello
    example-component

block script
    script(type='text/javascript').
        const foo = true;
        let bar = function() {};
        if (foo) {
            bar(1 + 5)
        }